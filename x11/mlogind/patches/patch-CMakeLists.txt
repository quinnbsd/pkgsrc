$NetBSD$

--- CMakeLists.txt.orig	2020-09-15 04:00:52.000000000 +0000
+++ CMakeLists.txt
@@ -25,7 +25,8 @@ set(MLOGIND_VERSION "${MLOGIND_VERSION_M
 set(CMAKE_INSTALL_PREFIX "/usr/local" CACHE PATH "Installation Directory")
 set(PKGDATADIR "${CMAKE_INSTALL_PREFIX}/share/mlogind")
 if(${CMAKE_SYSTEM_NAME} MATCHES "FreeBSD" OR
-		${CMAKE_SYSTEM_NAME} MATCHES "MidnightBSD"
+		${CMAKE_SYSTEM_NAME} MATCHES "MidnightBSD" OR
+		${CMAKE_SYSTEM_NAME} MATCHES "QuinnBSD"
 		)
 	set(SYSCONFDIR "${CMAKE_INSTALL_FULL_SYSCONFDIR}")
 	set(LIBDIR "${CMAKE_INSTALL_FULL_LIBDIR}")
@@ -41,6 +42,7 @@ if(${CMAKE_SYSTEM_NAME} MATCHES "FreeBSD
 		${CMAKE_SYSTEM_NAME} MATCHES "MidnightBSD" OR
 		${CMAKE_SYSTEM_NAME} MATCHES "MirBSD" OR
 		${CMAKE_SYSTEM_NAME} MATCHES "NetBSD" OR
+		${CMAKE_SYSTEM_NAME} MATCHES "QuinnBSD" OR
 		${CMAKE_SYSTEM_NAME} MATCHES "OpenBSD"
 		)
 	set(MLOGIND_DEFINITIONS ${MLOGIND_DEFINITIONS} "-DNEEDS_BASENAME")
@@ -117,7 +119,7 @@ find_package(PNG REQUIRED)
 find_package(ZLIB REQUIRED)
 
 # Fontconfig
-set(FONTCONFIG_DIR ${CMAKE_MODULE_PATH})
+set(FONTCONFIG_DIR "${CMAKE_SOURCE_DIR}/cmake/modules")
 find_package(FONTCONFIG REQUIRED)
 if(FONTCONFIG_FOUND)
 	message("\tFontConfig Found")
